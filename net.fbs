namespace network;

enum ID:uint16 {
  Verification,
  RVerification,

  SetNode,
  DeleteNode,
  ConnectNodes,
  UpdateNodeField,
  Export
}

table Packet {
  id: ID;
  flags: uint16;
  payload: [byte];
}
